# Provenance Guard

Agents fail fluently rather than loudly. Polluted context produces confident false output. Existing tools check the output and the trace but not the inputs. Provenance Guard gates both ends by classifying inbound chunks before context assembly and checking outbound claims against grounded support. The deliverable is a measured catch rate for context pollution and unsupported claims.
