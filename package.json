{
  "name": "provenance-guard",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.14.0",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm --filter @provguard/schema build && pnpm --filter @provguard/inbound build && pnpm --filter @provguard/outbound build && pnpm --filter @provguard/graph build && pnpm --filter @provguard/judge build && pnpm --filter @provguard/harness build && pnpm --filter @provguard/neo4j build && pnpm --filter @provguard/middleware build && pnpm --filter @provguard/cli build && pnpm --filter @provguard/demo build",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint .",
    "test": "vitest run",
    "format": "prettier --check .",
    "perf": "node scripts/perf.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.33.0",
    "@provguard/cli": "workspace:*",
    "@provguard/graph": "workspace:*",
    "@types/node": "^24.3.0",
    "eslint": "^9.33.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.39.1",
    "vitest": "^3.2.4"
  }
}
